[build]
  command = "hugo --gc --minify && npx -y pagefind@1.4.0 --site public"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.163.3"
  HUGO_ENV = "production"
  HUGO_ENABLEGITINFO = "true"
